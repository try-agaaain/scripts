# .config

# 网关地址，可以通过ip route等命令查看
GATEWAY="192.168.1.1"

# 网卡名称，必须手动指定
NETWORK_INTERFACE="enp0s3"

# 静态ip地址，必须手动指定
STATIC_IP="192.168.1.200/24"

# DNS服务器（多个地址用空格分隔，默认Google和阿里云DNS）
DNS_SERVERS="8.8.8.8 8.8.4.4 223.5.5.5 223.6.6.6"

# Netplan配置文件路径（默认为/etc/netplan/60-bridge-net.yaml）
CONFIG_FILE="/etc/netplan/60-bridge.yaml"
